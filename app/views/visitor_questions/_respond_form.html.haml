= form_for @question, :url => respond_ask_path(@question) do |f|
  .control-group
    = @question.email + " asked:"
  .control-group
    = @question.description
  .control-group
    = f.label :respond
    = f.text_area :respond
  .control-group
    = f.submit 'Respond'