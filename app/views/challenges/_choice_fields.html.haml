- if @question.type?('text')
  = f.text_field :content, :class => 'span12'
  = f.hidden_field :selected
- else
  - if @question.type?('single-select')
    = f.radio_button :selected, 'true', :title => 'Mark as answer', :class => 'pull-right span1'
  - else
    = f.check_box :selected, :title => 'Mark as answer', :class => 'pull-right span1'
  = f.link_to_remove 'remove', :class => 'btn pull-right'
  = f.text_field :content, :class => 'span-static'
  