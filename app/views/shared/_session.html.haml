%p.session.pull-right
  - if user_signed_in?
    Hello, <strong>#{current_user.email}</strong>
    |
    = link_to 'Sign out', destroy_user_session_path, :method => :delete
  - else
    = link_to 'Register', new_user_registration_path
    |
    \#{link_to 'Sign in', new_user_session_path}
 